package org.example;

import org.openqa.selenium.WebDriver;

public class BasePage
{

    //WebDriver declared - WebDriver is a tool for automating testing web application. WebDriver interact directly with browser

    protected static WebDriver driver;
}
